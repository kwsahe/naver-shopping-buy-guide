services:
  app:
    build: .
    ports:
      - "8400:8400"
    env_file: .env
    volumes:
      - ./data:/app/data
    command: python app.py

  scheduler:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
    command: python scheduler.py

